Create a new service file for fetching history records from Directus.

Requirements:

1. Create "src/services/history.ts".

2. Use the existing Directus SDK/client used elsewhere in the application.

3. Create TypeScript interface:

export interface HistoryRecord {
  id: number;
  user_created: string;
  date_created: string;
  module: string;
  record_id: string;
  action: string;
  summary: string;
  changed_fields: string[];
  before: Record<string, any>;
  after: Record<string, any>;
}

4. Add function:

export async function fetchHistory(recordId: string): Promise<HistoryRecord[]>

5. Fetch from Directus collection "history".

6. Filter records where:

record_id === recordId

7. Sort by:

date_created DESC

8. Return typed results.

9. Handle API failures gracefully and return empty array.


Refactor OwnershipTab to use Directus history data instead of mock/generated data.

Requirements:

1. Remove:
   
   - generateCaseOwnershipHistory()
   - all Math.random() usage
   - all hardcoded assignees

2. Load history records from:

fetchHistory(caseItem.id)

3. Only show records where:

changed_fields includes "owner"

4. Map Directus record into UI model:

{
  id: history.id.toString(),
  type: "reassignment",
  assignedFrom: history.before?.owner,
  assignedTo: history.after?.owner,
  assignedBy: history.user_created,
  assignedAt: history.date_created,
  reason: history.summary
}

5. Preserve current UI design exactly.

6. Preserve expand/collapse behavior.

7. Show loading state.

8. Show empty state when no ownership history exists.