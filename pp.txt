You are a senior frontend engineer and UI/UX designer working on a banking-grade internal application.

I have a Streamlit-based Databricks app called "Resource Cost Manager". The current UI is functional but looks cluttered, unstructured, and not production-ready. Your task is NOT to change functionality, but to significantly improve the UI/UX to match a modern banking/internal enterprise application.

Follow these strict guidelines:

1. Layout & Structure:
- Use a clean, centered layout with max-width (1200px–1400px), not full stretched UI
- Add proper padding (16px–24px) and spacing between components
- Group related elements into cards/sections with subtle shadows or borders
- Maintain consistent alignment (left-aligned labels, structured grids)

2. Design System:
- Use a minimal professional color palette:
  - Primary: deep blue or banking green
  - Secondary: neutral grays
  - Background: light gray (#f7f9fb or similar)
- Avoid excessive colors or bright tones
- Maintain consistent typography hierarchy:
  - Page Title
  - Section Headers
  - Labels
  - Inputs

3. Components Styling:
- Convert forms into structured sections (multi-column grid where needed)
- Use proper input widths (not full-width everywhere)
- Add spacing between fields
- Style buttons:
  - Primary (filled)
  - Secondary (outline)
- Add hover states and subtle transitions

4. Tables:
- Add zebra striping or subtle row separation
- Fix column alignment and padding
- Make header sticky if possible
- Add sorting/filter UI if feasible

5. Navigation:
- Convert top buttons (Resource Costs, Add Resource, Genie Assistant) into a proper tab or navbar layout
- Highlight active tab clearly
- Keep navigation fixed and clean

6. Forms (Add Resource Page):
- Break into logical sections:
  - Project Details
  - Resource Info
  - Cost Details
- Use 2-column responsive layout instead of long vertical scrolling
- Add section separators and titles

7. Genie Assistant Page:
- Make it look like a chat interface:
  - Message bubbles
  - Input box fixed at bottom
  - Suggested queries as clickable chips/cards
- Improve readability and spacing

8. UX Improvements:
- Reduce empty whitespace and overly stretched components
- Ensure consistent margins and paddings across pages
- Avoid clutter — simplify wherever possible
- Make it look like an internal enterprise banking dashboard (similar to tools used in banks)


Now refactor this UI into reusable components (form_section, table_wrapper, navbar, card_container) so that the design stays consistent across all pages.

9. Streamlit-specific improvements:
- Use st.container(), st.columns(), st.tabs() properly
- Use custom CSS (via st.markdown with unsafe_allow_html=True) for styling
- Override default Streamlit spacing and width behavior

10. Output Expectations:
- Provide updated Streamlit code with clean structure
- Include reusable layout helpers/components
- Add CSS styling block separately
- Ensure code is modular and readable

Goal:
The final UI should look like a polished internal banking application — clean, structured, minimal, and professional — NOT like a demo or prototype.