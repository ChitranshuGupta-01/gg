I have already implemented secure download functionality for artifacts in my Next.js + TypeScript project using MinIO.

Current architecture:

- Route: src/app/api/artifacts/[id]/download/route.ts
- Service: ArtifactDownloadService.getDownloadPayload(id)
- Storage helper: getObjectFromMinio(storageKey)
- Files are stored in MinIO.
- Metadata (storage_key, mime_type, content_type, original_filename, etc.) is stored in Directus.
- The service returns:
  {
    stream,
    size,
    contentType,
    filename
  }

The download route streams the file back using NextResponse with:
- Content-Type
- Content-Length
- Content-Disposition: attachment

I now want to implement PREVIEW functionality while reusing as much existing code as possible.

Requirements:
1. Do NOT duplicate business logic.
2. Reuse ArtifactDownloadService.getDownloadPayload().
3. Reuse getObjectFromMinio().
4. Create a new route:
   src/app/api/artifacts/[id]/preview/route.ts
5. Stream the file exactly like download.
6. Use Content-Disposition: inline instead of attachment.
7. Preserve Content-Type and Content-Length.
8. Support browser preview for images, PDFs, videos, and audio.
9. Return proper HTTP errors exactly like the download route.
10. Keep the implementation clean and enterprise-ready.

Please generate the complete implementation and explain every change.