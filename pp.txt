Redesign this Streamlit app UI to look like a modern enterprise banking dashboard (similar to Stripe / fintech admin panels).

STRICT REQUIREMENTS:

1. Layout:

- Add a LEFT SIDEBAR navigation (fixed)
  - Resource Costs
  - Add Resource
  - Genie Assistant
- Main content should be card-based, not full-width stretched

2. Top Section:

- Add a HEADER BAR with:
  - App title (Resource Cost Manager)
  - User profile icon (top right)
  - Subtle shadow

3. Design System:

- Use a clean color palette:
  - Primary: #006A4D (bank green)
  - Background: #F5F7FA (light grey)
  - Cards: white with soft shadow
- Rounded corners (border-radius: 12px)
- Add spacing (padding: 16px–24px)

4. Resource Costs Page:

- Convert filters into a CARD (not stretched row)
- Add KPI cards at top:
  - Total Cost
  - Active Resources
  - Avg Cost per Resource
- Table should be inside a card with:
  - Sticky header
  - Zebra rows
  - Hover effect

5. Add Resource Page:

- Convert form into multi-step form (tabs or sections)
- Group fields logically
- Align inputs properly in grid (2 columns)

6. Genie Assistant:

- Make it look like CHAT UI:
  - Chat bubbles (user vs system)
  - Input box fixed at bottom
  - Suggested questions as clickable pills
- Show SQL in collapsible card

7. UX Improvements:

- Add icons everywhere (use emojis or simple icons)
- Add hover effects on buttons
- Buttons should be rounded + elevated
- Reduce empty whitespace

8. Typography:

- Use larger headings
- Clear section separation

GOAL:
Make it look like a production-grade fintech dashboard, not a basic Streamlit app.