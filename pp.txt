Refactor the UI to reduce excessive top spacing and improve vertical alignment. Do NOT redesign anything else.

STRICT CHANGES:

1. Reduce Top Space:

- Remove excessive margin/padding above the main content
- Bring the page title ("Resource Costs") closer to the top
- Ensure content starts immediately below the header/sidebar

2. Streamlit Layout Fix:

- Reduce default top padding of the main container
- Remove unnecessary empty space introduced by layout containers

3. Alignment:

- Align KPI cards directly below the page title
- Maintain consistent vertical spacing between:
  - Title
  - KPI cards
  - Filters
  - Table

4. Spacing Rules:

- Use tight spacing (8px–16px max between sections)
- Avoid large empty gaps

5. Do NOT:

- Do NOT add new components
- Do NOT change colors, layout, or structure
- Do NOT modify sidebar or cards
- Only fix spacing and alignment

GOAL:
Make the UI compact, tight, and professionally aligned like a fintech dashboard.