Refactor the existing UI to REMOVE clutter and improve cleanliness. Do NOT add new components. Only simplify and refine.

STRICT CHANGES:

1. Sidebar Fix:

- Keep ONLY one navigation menu (left sidebar)
- REMOVE duplicate buttons like "Resource Costs", "Add Resource", "Genie Assistant" from main content area

2. Icons:

- Reduce icons by 70%
- Keep icons ONLY in:
  - Sidebar menu
  - KPI cards (optional, small size)
- REMOVE icons from:
  - Buttons
  - Section headers
  - Input labels

3. Spacing:

- Reduce top margin significantly
- Bring content closer to header
- Maintain consistent padding (16px–20px)

4. Buttons:

- Keep buttons simple (no icons inside buttons)
- Use consistent size and style
- Avoid large bulky buttons

5. Layout Simplification:

- Remove unnecessary extra cards/containers
- Keep:
  - KPI cards (top)
  - Filters section
  - Table
- Ensure everything aligns in a clean grid

6. Typography:

- Reduce excessive headings
- Keep only one main page title
- Use smaller subheadings

7. Genie Assistant:

- Keep it minimal
- Remove extra decorative elements
- Focus on clean chat + input box

8. Overall Goal:
   Make the UI feel clean, minimal, and professional like a fintech dashboard.
   Avoid over-design, avoid duplication, avoid excessive icons.

IMPORTANT:
Do not add new features. Only clean and refine the existing UI.