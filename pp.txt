Improve only the styling and layout of the Sort dropdown in the Cases panel.

Requirements:
- Do NOT change any functionality or sorting logic.
- Do NOT change state management, event handlers, or the sortCases function.
- Only modify the JSX structure and Tailwind CSS classes if needed.

UI goals:
1. Make the Sort control visually consistent with the existing search box, filter button, and filter chips.
2. Reduce its width so it doesn't dominate the row (around 140-160px).
3. Align the Sort control to the right side of the row while keeping the "X cases found" text on the left using flex and justify-between.
4. Match the height, border, border radius, padding, and typography of the surrounding controls.
5. Improve spacing between the sort icon, text, and dropdown arrow.
6. Ensure the control looks clean in both light and dark themes.
7. Keep the component responsive and avoid layout shifts.
8. Do not introduce any new dependencies or custom CSS. Use existing Tailwind utility classes already used in this file.

Return only the minimal JSX and Tailwind class changes with a brief explanation of why each change improves the UI.