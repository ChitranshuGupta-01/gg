Analyze the entire codebase and identify all coding patterns, conventions, and architectural decisions currently being followed.

Create a GitHub Copilot instruction file (.github/copilot-instructions.md) that future AI agents must follow when generating code.

Include:

- Folder structure conventions
- Naming conventions for files, classes, interfaces, types, DTOs, services, controllers, repositories, utilities, and constants
- API implementation patterns
- Directus integration patterns
- Error handling approach
- Logging conventions
- Dependency injection patterns
- Static method usage rules
- Async/await usage standards
- TypeScript typing standards
- Import ordering conventions
- Database access patterns
- Validation patterns
- Response formatting standards
- Testing patterns
- Code organization principles

Important requirements:

- Analyze the existing codebase first before creating the file.
- Use only conventions that already exist in the project.
- Do not suggest improvements, refactoring, or modern alternatives.
- Do not invent new patterns.
- If multiple patterns exist, prefer the pattern used by the majority of production code.
- The generated instruction file should be detailed enough that future AI-generated code is indistinguishable from existing project code.
- Save the result as .github/copilot-instructions.md.