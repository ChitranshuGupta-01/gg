Review the current sorting implementation. Since priority, severity, urgency, and risk_score are stored as numeric values in the database, move sorting for these fields to the database/Directus GraphQL query instead of performing it in route.ts. Keep custom sorting logic only for status, since it is stored as text and requires a business-defined order